<% include partials/header %>
Blog posts
<% blogs.forEach((blog) => { %>
    <h1><%= blog.title %></h1>
    <div><%= blog.created %></div>
    <img src="<%= blog.image %>">
    <p><%= blog.body %></p>
<%});%>

<% include partials/footer %>