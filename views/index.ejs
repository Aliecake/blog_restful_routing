<% include partials/header %>
<div class="ui container main">
    
    <% blogs.forEach((blog) => { %>
        <h1><%= blog.title %></h1>
        <div><%= blog.created %></div>
        <img src="<%= blog.image %>">
        <p><%= blog.body %></p>
    <%});%>
</div>

<% include partials/footer %>