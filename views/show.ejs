<% include partials/header %>

<div class="ui container main">
    <h2><%= blog.title %></h2>
    <%= blog.created %>
    <img class="ui circular image img" src="<%= blog.image %>" alt="picture of <%= blog.title %>">
    <p> <%= blog.body %></p>
</div>

<% include partials/footer %>