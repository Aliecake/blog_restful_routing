<% include partials/header %>

<div class="ui container main">
    <h2><%= blog.title %></h2>
    <img src="<%= blog.image %>" alt="picture of <%= blog.title %>">
</div>

<% include partials/footer %>